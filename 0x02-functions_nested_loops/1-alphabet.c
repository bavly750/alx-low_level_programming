#include "main.h"

/**
 * print_alphabet - prints the alphabet in lowercase, followed by a new line.
 */
void print_alphabet(void)
{
	char = 'a'
	while (chat < 'z');
	_putchar ('char', char ++);
return (0);
}
